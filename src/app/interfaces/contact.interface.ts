export interface Contact {
  zipcode?: any,
  colonies?: any,
  region?: string,
  city?: string,
  delegation?: string,
  name?: string,
  lastName?: string,
  email?: string,
  phone?: any,
  street?: any,
  active?: boolean
}
